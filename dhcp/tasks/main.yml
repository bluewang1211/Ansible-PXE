---

- name: install dhcp server
  yum:
    name: dhcp
    state: present

- name: copy dhcp config to remote host
  template:
    src: dhcpd.conf.j2
    dest: /etc/dhcp/dhcpd.conf
    owner: root
    group: root
    mode: 0644
    backup: yes
  notify: Enable and Restart DHCP Server
